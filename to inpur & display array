#include<stdio.h>
int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int i,x;
    scanf("%d",&x);
    int list[x];
    for (i=0; i<x; i=i+1){
        scanf("%d ",&list[i]);
    }
    for(int i = 0; i < x; i++) {
        printf("%d ", list[i]);
    }

    printf("\n");
    return 0;
}
